unclassified:
  location:
    url: http://localhost
    adminAddress: support@praqma.net
  gitSCM:
    createAccountBasedOnEmail: false
    globalConfigEmail: "jenkins@jenkins.com"
    globalConfigName: "Jenkins"
  
  globalLibraries:
    libraries:
      - name: "common-library@master"
        implicit: true
        defaultVersion: "master"
        retriever:
          modernSCM:
            scm:
              git:
                remote: "https://github.com/alexsedova/common-library.git"
                